version: 1
backend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn build
        - amplifyPush --simple
frontend:
  artifacts:
    baseDirectory: build